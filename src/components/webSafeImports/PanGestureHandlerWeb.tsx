import React from 'react'
import {PanGestureHandlerProps} from 'react-native-gesture-handler'

const PanGestureHandlerWeb: React.FC<PanGestureHandlerProps> = ({children}) => {
  return <>{children}</>
}

export default PanGestureHandlerWeb
